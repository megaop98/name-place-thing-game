- uses: huggingface/hub-sync@v0.1.0
  with:
    github_repo_id: megaop98/name-place-thing-game
    huggingface_repo_id: megaop98/name-place-thing-game  # Make sure this matches your actual HF Space name
    hf_token: ${{ secrets.HF_TOKEN }}
